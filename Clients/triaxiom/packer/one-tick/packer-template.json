{
  "variables": {
          "aws_access_key":"",
          "aws_secret_key":"",
          "ftp_password":""
  },
  "builders": [{
          "type": "amazon-ebs",
          "access_key": "{{user `aws_access_key`}}",
          "secret_key": "{{user `aws_secret_key`}}",
          "region": "us-west-1",
          "instance_type": "t2.micro",
          "ssh_username": "ec2-user",
          "source_ami": "ami-01c94064639c71719",
          "ami_name": "ami-onetick {{timestamp}}"
  }],
  "provisioners": [
    {
      "type": "shell",
      "environment_vars": [
        "ftp_password={{user `ftp_password`}}"
      ],
      "scripts": [
        "scripts/setup.sh"
      ]
    }
  ]
}